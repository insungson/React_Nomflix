import TVContainer from './TVContainer';

export default TVContainer;
// 컨테이너는 data 를 가지고, state(상태값)를 가지고 api를 불러와서 로직을 처리하고
// 프리젠터는 state(상태값)을 가지지 않고, api도 없고 클래스도 없는 그냥 화면을 보여주는
// 함수형 컴포넌트이다!!
// 프리젠터는 스타일
// 컴포넌트는 데이터 라고 생각하면 편하다